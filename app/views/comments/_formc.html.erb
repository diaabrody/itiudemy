<%= form_for ([@lecture.comments.build]) do |f| %>

  <div class="field" >

    <%= f.text_area :body %>
  </div>



  <%= hidden_field_tag 'lecture_id', @lecture.id  %>
  <div class="actions">
    <%= f.submit  class: "btn btn-success" %>
  </div>
<% end %>